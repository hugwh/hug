server:
  port: 8002
spring:
  application:
    name: hug-demo-client
  cloud:
    config:
#      uri: http://localhost:8888
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: hug-config
    profiles:
      active: dev