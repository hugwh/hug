server:
  port: 8003
spring:
  application:
    name: hug-demo-server
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: hug-config
    profiles:
      active: dev